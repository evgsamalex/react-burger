import React from 'react';
import withPage from "../providers/with-page";

const Ingredients = () => {
  return (
    <div>
      Ingredients
    </div>
  );
};

export default withPage(Ingredients);
